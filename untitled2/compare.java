public interface comparable<object> {
    int compareto(object A);
    object max;

}
}

!@override
public class Dog implements comparable<object> {

    @Override
    public int object compareto(object A) {
        return data-A.data;
    }

    public object compareto(object A) {
        DOg d=9Dog) A;
        return null;
    }
}

mian()

    public object max(object[] data)
    Dog[] A;

    Dog maxDog=A[0];
    for(int i=0;)
    maxDog.compareto(Dog[i]);

    return maxDog;

public static comparable max(comparable[] items) {
    Dog temp=new Dog;
    for(int i=0;i<items.length;i++) {
        tmep=compareto();

        }
    return Dog;
        }
